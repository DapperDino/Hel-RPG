namespace Hel.Combat
{
    /// <summary>
    /// Defines how a stat modifier should be used in calculation.
    /// </summary>
    public enum StatModifierType
    {
        None = -1,
        Flat = 100,
        PercentageAdditive = 200,
    }
}